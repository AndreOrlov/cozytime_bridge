# CozyTime BLE Bridge для Home Assistant

ESPHome компонент для интеграции термогигрометра CozyTime с Home Assistant через ESP32 BLE.

## 📋 Возможности

- ✅ Считывание температуры (в °F)
- ✅ Считывание влажности (%)
- ✅ Считывание заряда батареи (%)
- ✅ Мониторинг силы сигнала (RSSI)
- ✅ Доступ к сырым данным температуры
- ✅ Автоматическое обновление каждые 5 секунд

## 🔧 Требования

- **Оборудование**: ESP32 (любая модель с Bluetooth)
- **Программное обеспечение**: ESPHome 2023.x или новее
- **Home Assistant**: Любая современная версия

## 📡 Формат BLE пакета CozyTime

### Структура BLE Advertisement

```
BLE Advertisement Packet
│
├─ Manufacturer UUID:  0x51C9 (не входит в data)
└─ Manufacturer Data:  [массив байтов с полезными данными]
```

### Карта байтов Manufacturer Data

| Байт | Значение | Описание | Формула |
|------|----------|----------|---------|
| `[0-3]` | `CE CD 6C CE` | Фиксированный префикс (идентификатор CozyTime) | - |
| `[4]` | `??` | Неизвестно | - |
| `[5]` | **ТЕМПЕРАТУРА** | Сырое значение температуры | `(byte[5] × 0.2) + 49.7 = °F` |
| `[6]` | `??` | Неизвестно | - |
| `[7]` | **ВЛАЖНОСТЬ** | Влажность в % | `byte[7] = humidity %` |
| `[8]` | **БАТАРЕЯ** | Заряд батареи в % | `byte[8] = battery %` |
| `[9-13]` | `??` | Неизвестно | - |

### Пример реального пакета

```
CE CD 6C CE ?? 78 ?? 2A 55 ?? ?? ?? ?? ??
│           │  │  │  │  │
│           │  │  │  │  └─ Byte[8]: Батарея = 85%
│           │  │  │  └──── Byte[7]: Влажность = 42%
│           │  │  └─────── Byte[6]: (неизвестно)
│           │  └────────── Byte[5]: Temp = 120 → (120×0.2)+49.7 = 73.7°F
│           └───────────── Byte[4]: (неизвестно)
└───────────────────────── Префикс CozyTime (4 байта)
```

## 🚀 Установка

### 1. Подготовка файлов

Создайте структуру проекта:

```
cozytime_bridge/
├── cozytime_bridge.yaml    # Основная конфигурация
├── cozytime_parser.h        # Парсер (опционально, если нужен)
└── secrets.yaml             # Секреты (WiFi пароли)
```

### 2. Настройка secrets.yaml

```yaml
wifi_ssid: "ваш_wifi_ssid"
wifi_password: "ваш_wifi_пароль"
```

### 3. Первая прошивка через USB

```bash
esphome upload cozytime_bridge.yaml --device /dev/cu.usbserial-10
```

### 4. Последующие обновления через WiFi (OTA)

```bash
esphome upload cozytime_bridge.yaml
```

## 📊 Сенсоры в Home Assistant

После успешного подключения в HA появятся следующие сенсоры:

| Entity ID | Название | Единица | Описание |
|-----------|----------|---------|----------|
| `sensor.cozytime_temperature` | CozyTime Temperature | °F | Температура в Фаренгейтах |
| `sensor.cozytime_temperature_raw` | CozyTime Temperature RAW | - | Сырое значение byte[5] |
| `sensor.cozytime_humidity` | CozyTime Humidity | % | Влажность |
| `sensor.cozytime_battery` | CozyTime Battery | % | Заряд батареи |
| `sensor.cozytime_rssi` | CozyTime RSSI | dBm | Сила сигнала |

## 🔬 Формулы преобразования

### Температура

```cpp
float temperature = (temp_byte * 0.2) + 49.7;  // °F
```

**Особенности:**
- Шаг измерения: 0.2°F (высокая точность)
- Датчик изначально работает в Фаренгейтах
- Для конвертации в Цельсии в Home Assistant: Settings → Devices → Entity → Unit of Measurement

**Пример:**
- `byte[5] = 120` → `(120 × 0.2) + 49.7 = 73.7°F` → `~23.2°C`

### Влажность

```cpp
float humidity = humid_byte;  // Прямое значение
```

**Особенности:**
- Прямое значение без преобразования
- Диапазон: 0-100%

### Батарея

```cpp
int battery = battery_byte;  // Прямое значение
```

**Особенности:**
- Прямое значение без преобразования
- Диапазон: 0-100%

## ⚙️ Настройка параметров BLE сканирования

В файле `cozytime_bridge.yaml`:

```yaml
esp32_ble_tracker:
  scan_parameters:
    interval: 5s       # Интервал между сканированиями
    window: 1.1s       # Длительность окна сканирования
    active: true       # Активное сканирование
```

**Рекомендации:**
- `interval: 5s` - оптимальный баланс между частотой обновления и энергопотреблением
- `window: 1.1s` - достаточно для обнаружения сигнала
- Слишком частое сканирование может привести к зависанию ESP32

## 🐛 Решение проблем

### ESP32 зависает / перезагружается

**Причина:** Слишком интенсивное BLE сканирование или избыточное логирование

**Решение:**
```yaml
logger:
  level: WARN  # Снизить уровень логирования

esp32_ble_tracker:
  scan_parameters:
    interval: 10s  # Увеличить интервал
```

### Сенсоры показывают 0.0

**Причина:** CozyTime датчик не обнаружен

**Решение:**
1. Проверьте, что датчик включен и рядом с ESP32
2. Включите debug логирование временно:
   ```yaml
   logger:
     level: DEBUG
   esp32_ble_tracker:
     log_level: DEBUG
   ```
3. Проверьте логи на наличие устройств с префиксом `CE CD 6C CE`

### Home Assistant показывает неправильную температуру

**Причина:** Автоматическая конвертация единиц измерения

**Решение:**
- Удалите `device_class: temperature` из конфигурации сенсора
- В Home Assistant: Settings → Devices → Entity → Edit → Change unit manually

### OTA обновление не работает

**Причина:** ESP32 не отвечает по сети

**Решение:**
1. Проверьте, что ESP32 подключен к WiFi
2. Попробуйте перезагрузить ESP32
3. Используйте USB для прошивки

## 📝 Логирование и отладка

### Просмотр логов в реальном времени

```bash
esphome logs cozytime_bridge.yaml
```

### Полезные уровни логирования

```yaml
logger:
  level: DEBUG  # Для отладки (много информации)
  level: INFO   # Нормальный режим
  level: WARN   # Минимум логов (стабильность)
```

## 📄 Структура проекта

```
cozytime_bridge/
├── cozytime_bridge.yaml    # Главная конфигурация ESPHome
├── cozytime_parser.h        # Custom компонент (опционально)
├── secrets.yaml             # WiFi credentials
├── README.md                # Документация
└── .gitignore               # Исключения для git
```

## 🎯 Примеры автоматизаций в Home Assistant

### Уведомление о низком заряде батареи

```yaml
automation:
  - alias: "CozyTime Low Battery Alert"
    trigger:
      - platform: numeric_state
        entity_id: sensor.cozytime_battery
        below: 20
    action:
      - service: notify.mobile_app
        data:
          message: "CozyTime battery is low: {{ states('sensor.cozytime_battery') }}%"
```

### Отображение в Lovelace Card

```yaml
type: entities
title: CozyTime Sensor
entities:
  - entity: sensor.cozytime_temperature
    name: Temperature
  - entity: sensor.cozytime_humidity
    name: Humidity
  - entity: sensor.cozytime_battery
    name: Battery
  - entity: sensor.cozytime_rssi
    name: Signal Strength
```

## 🔗 Полезные ссылки

- [ESPHome Documentation](https://esphome.io/)
- [ESP32 BLE Tracker](https://esphome.io/components/esp32_ble_tracker.html)
- [Home Assistant](https://www.home-assistant.io/)

## 📜 Лицензия

MIT License - свободное использование для личных и коммерческих целей.

## 🙏 Благодарности

Проект создан в результате реверс-инжиниринга протокола CozyTime BLE устройства.

---

**Версия:** 1.0
**Последнее обновление:** Декабрь 2025
**Автор:** @andreyorlov
